package com.example.restaurant_order_portal.enums;

/**
 * Enum representing roles of users in the system.
 *
 * This is used for authorization and role-based access control.
 */
public enum Role {
    USER,
    RESTAURANT_OWNER
}
